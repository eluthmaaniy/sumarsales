# This project currently does not read any application environment variables.
# No VITE_* keys are referenced in source. Leave this file as a template if
# you add secrets later, and set the same names in the Vercel dashboard.

# Example (uncomment if you add client-side config):
# VITE_PUBLIC_SITE_URL=https://your-domain.vercel.app
